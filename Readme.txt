Run test2.py to run for classic4 dataset. The code is commented with suitable instuctions. The document choosing size can be varied at the start of the program. Benchmarks can be altered later in the program. HOWEVER, a new computer must run the following commands to get natural-language-toolkit modules.

>>sudo pip install stemming
>>python
	>>import nltk
	>>nltk.download(“stopwords”)
	>>nltk.download(“wordnet”)
	>>nltk.download(“maxent_treebank_pos_tagger”)

test20.py runs the 20ng dataset. Testreuters1.py runs the Reuters dataset